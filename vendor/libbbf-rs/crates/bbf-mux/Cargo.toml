[package]
name = "bbf-mux"
version = "0.1.0"
edition = "2024"
description = "BBF muxing primitives"
license = "MIT"

[features]
component-bench = []

[dependencies]
bbf-format = { path = "../bbf-format" }
bbf-io = { path = "../bbf-io" }
xxhash-rust = { version = "0.8", features = ["xxh3"] }

[lints.rust]
unsafe_code = "forbid"
