For any file search or grep in the current git-indexed directory, use fff and ccc tools/skills.

Never update the NixOS system with `nixos-rebuild switch`, `deploy-rs`, or any other direct switch path. The only allowed NixOS update path is systemd sysupdate.

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `TrashCan69420/manga-downloader`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default five-label triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Use a multi-context domain-doc layout with root `CONTEXT-MAP.md`. See `docs/agents/domain.md`.
