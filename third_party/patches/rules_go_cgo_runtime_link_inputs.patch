diff --git a/go/private/rules/cgo.bzl b/go/private/rules/cgo.bzl
index 7931b924..1e9417c9 100644
--- a/go/private/rules/cgo.bzl
+++ b/go/private/rules/cgo.bzl
@@ -91,6 +91,7 @@ def cgo_configure(go, srcs, cdeps, cppopts, copts, cxxopts, clinkopts):
     inputs_transitive = []
     deps_direct = []
     link_inputs_direct = []
+    link_inputs_transitive = []
     lib_opts = []
     runfiles = go._ctx.runfiles(collect_data = True)
 
@@ -178,9 +179,15 @@ def cgo_configure(go, srcs, cdeps, cppopts, copts, cxxopts, clinkopts):
         else:
             fail("unknown library has neither cc nor objc providers: %s" % d.label)
 
+    if needs_cxx_runtime:
+        inputs_transitive.append(runtime_libs)
+        inputs_transitive.append(go.cc_toolchain_files)
+        link_inputs_transitive.append(runtime_libs)
+        link_inputs_transitive.append(go.cc_toolchain_files)
+
     inputs = depset(direct = inputs_direct, transitive = inputs_transitive)
     deps = depset(direct = deps_direct)
-    link_inputs = depset(direct = link_inputs_direct)
+    link_inputs = depset(direct = link_inputs_direct, transitive = link_inputs_transitive)
 
     # HACK: some C/C++ toolchains will ignore libraries (including dynamic libs
     # specified with -l flags) unless they appear after .o or .a files with
diff --git a/go/private/actions/link.bzl b/go/private/actions/link.bzl
index 465d7577..cf812035 100644
--- a/go/private/actions/link.bzl
+++ b/go/private/actions/link.bzl
@@ -186,6 +186,7 @@ def emit_link(
         archive.cgo_link_inputs,
         go.cc_toolchain_files,
         go.sdk.tools,
+        go.stdlib.link_inputs,
         go.stdlib.libs,
     ]
     inputs = depset(direct = inputs_direct, transitive = inputs_transitive)
diff --git a/go/private/actions/stdlib.bzl b/go/private/actions/stdlib.bzl
index b46cc0d0..f4378984 100644
--- a/go/private/actions/stdlib.bzl
+++ b/go/private/actions/stdlib.bzl
@@ -123,5 +123,6 @@ def _sdk_stdlib(go):
         _list_json = list_json,
         cache_dir = depset([cache_dir]),
         libs = go.sdk.libs,
+        link_inputs = go.cc_toolchain_files,
         root_file = go.sdk.root_file,
     )
 
diff --git a/go/private/actions/stdlib.bzl b/go/private/actions/stdlib.bzl
index b46cc0d0..f4378984 100644
--- a/go/private/actions/stdlib.bzl
+++ b/go/private/actions/stdlib.bzl
@@ -180,5 +181,6 @@ def _build_stdlib(go):
         _list_json = list_json,
         libs = depset([pkg]),
         cache_dir = depset([cache_dir]),
+        link_inputs = go.cc_toolchain_files,
         root_file = pkg,
     )
diff --git a/go/tools/builders/env.go b/go/tools/builders/env.go
index 43b1c298..63ad1762 100644
--- a/go/tools/builders/env.go
+++ b/go/tools/builders/env.go
@@ -210,8 +210,17 @@ func absCCLinker(argList []string) (func(), error) {
 func absCCLinker(argList []string) (func(), error) {
 	extldIndex := -1
+	extldValue := ""
+	extldUsesEquals := false
 	for i, arg := range argList {
 		if arg == "-extld" && i+1 < len(argList) {
 			extldIndex = i + 1
+			extldValue = argList[extldIndex]
+			break
+		}
+		if strings.HasPrefix(arg, "-extld=") {
+			extldIndex = i
+			extldValue = strings.TrimPrefix(arg, "-extld=")
+			extldUsesEquals = true
 			break
 		}
 	}
@@ -228,7 +236,7 @@ func absCCLinker(argList []string) (func(), error) {
 		return nil, err
 	}
 
-	err = os.Setenv("GO_CC", argList[extldIndex])
+	err = os.Setenv("GO_CC", extldValue)
 	if err != nil {
 		cleanup()
 		return nil, err
@@ -240,7 +248,11 @@ func absCCLinker(argList []string) (func(), error) {
 		return nil, err
 	}
 
-	argList[extldIndex] = buildercc
+	if extldUsesEquals {
+		argList[extldIndex] = "-extld=" + buildercc
+	} else {
+		argList[extldIndex] = buildercc
+	}
 
 	return cleanup, nil
 }
